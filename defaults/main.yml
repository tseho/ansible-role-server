---

server_php_dependencies:
  - apt-transport-https
  - ca-certificates

server_tools:
  - git
  - acl
  - jpegoptim
  - optipng

server_writable_paths: []
# server_writable_paths:
#   - path: "/var/foo"
#     user: "www-data"
